import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.vocapp',
  appName: 'vocapp',
  webDir: 'dist/vocapp/browser',
};

export default config;
